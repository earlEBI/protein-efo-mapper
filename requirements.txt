# Python dependencies for this workspace
openpyxl==3.1.5
pandas==3.0.0
tqdm==4.67.3
pillow==12.1.1
pytesseract==0.3.13
pdf2image==1.17.0
fastapi==0.117.0
uvicorn[standard]==0.36.0
python-multipart==0.0.20

# System dependencies (install separately; not pip packages):
# - tesseract OCR engine (macOS: `brew install tesseract`)
# - poppler utilities for PDF rasterization (macOS: `brew install poppler`)
